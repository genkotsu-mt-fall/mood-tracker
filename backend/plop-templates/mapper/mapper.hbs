import { {{pascalCase name}} as Prisma{{pascalCase name}} } from '@prisma/client';
import { {{pascalCase name}}Entity } from '../entity/{{kebabCase name}}.entity';
import { PrivacySetting } from '../type/privacy-setting.type';

export function to{{pascalCase name}}Entity(data: Prisma{{pascalCase name}}): {{pascalCase name}}Entity {
  return new {{pascalCase name}}Entity(
    data.id,
    data.userId,
    data.mood,
    data.intensity,
    data.body,
    data.emoji,
    data.templateId,
    data.privacyJson as PrivacySetting | null,
    data.followersOnly,
    data.followBackOnly,
    data.minFollowDays,
    data.visibleUntil,
    data.visibleAfter,
    data.crisisFlag,
    data.createdAt,
    data.updatedAt,
  );
}
